<% content_for(:title, '口コミ検索') %>

<%= render 'search_form', q: @q, url: reviews_path %>

<%# spotデータ(一覧表示) %>
<div class="w-5/6 mx-auto max-w-sm md:max-w-4xl">
    <div class="mt-10 mb-28">
      <% if @reviews.present? %>
        <%= render @reviews %>
      <% else %>
        <p>データがありません</p>
      <% end %>
    </div>
</div>
